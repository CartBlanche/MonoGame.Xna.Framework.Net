namespace Microsoft.Xna.Framework.Net.iOS
{
    /// <summary>
    /// Controls whether iOS integration fails fast or falls back to local providers.
    /// </summary>
    public enum IOSFallbackMode
    {
        PreferFallback,
        Strict
    }
}