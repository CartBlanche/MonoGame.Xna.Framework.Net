namespace Microsoft.Xna.Framework.Net.Android
{
    /// <summary>
    /// Controls whether Android integration fails fast or falls back to local providers.
    /// </summary>
    public enum AndroidFallbackMode
    {
        PreferFallback,
        Strict
    }
}
